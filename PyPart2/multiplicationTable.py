for multiplicand in range(1, 11):
  for multiplier in range(1, 11):
    product = multiplicand * multiplier
    print(str(multiplicand) + ' X ' + str(multiplier) + ' = ' + str(product))

# 1 to 10
# 2 to 10
# 3 to 10